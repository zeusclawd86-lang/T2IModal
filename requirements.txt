# Modal + dependencias mínimas para ejecutar localmente (modal run)
modal>=0.64.0

# Para API HTTP (modal serve) y resolución de tipos local
fastapi[standard]>=0.115.0
pydantic>=2.0

# Calidad tipo Replicate (incluidos en la imagen Modal; opcional local)
# compel>=2.0.0
# mediapipe>=0.10.0
